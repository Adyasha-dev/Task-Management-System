# Task-Management-System

Task Management application built using MongoDB, Express.js, React.js, and Node.js with secure authentication and task organization features.
1.Set up the project folder structure with separate frontend and backend directories.
2.Initialized the backend using Node.js and Express.js.
3.Connected the backend to MongoDB Atlas using Mongoose.
4.Configured environment variables using the .env file.
